package fu.stockspace.stockspace_be.contract.entity;













public enum ContractStatus {
    DRAFT,
    PENDING_TENANT_CONFIRM,
    CHANGES_REQUESTED,
    SCHEDULED,
    ACTIVE,
    REJECTED,
    EXPIRED
}
