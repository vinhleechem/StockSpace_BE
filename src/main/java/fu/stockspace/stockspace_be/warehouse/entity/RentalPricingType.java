package fu.stockspace.stockspace_be.warehouse.entity;

/** Defines how a warehouse rental price is interpreted. */
public enum RentalPricingType {
    FIXED_MONTHLY,
    PER_SQUARE_METER_MONTHLY,
    NEGOTIATED
}
